in python, we create modules, and we create class only we really need object, otherwise,
modules are enough for many things;
created module
directory for unrelated files
package for related files: need constructor for everything in the package